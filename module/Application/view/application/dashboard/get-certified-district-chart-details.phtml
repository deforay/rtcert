
<div id="certifiedDistrict" class="ct-chart"></div>
<script>
    Highcharts.chart('certifiedDistrict', {
    chart: {
        plotBackgroundColor: null,
        plotBorderWidth: null,
        plotShadow: false,
        type: 'pie'
    },
    credits: {
        enabled: false
    },
    title: {
        text: ''
    },
    exporting:{
        chartOptions:{
            subtitle: {
                text:'',
            }
        }
    },
    tooltip: {
        pointFormat: '{series.name}: <b>({point.number})</b>'
    },
    plotOptions: {
        pie: {
            //allowPointSelect: true,
            cursor: 'default',
            dataLabels: {
                enabled: false,
                distance: -60
            },
            showInLegend: true
        }
    },
    series: [{
        name: 'Total Certified Testers',
        colorByPoint: true,
        data: [
        <?php  if(isset($result) && count($result) > 0) {
            foreach($result as $region){
            ?>
        {
            name: '<?php echo ucfirst($region['location_name']); ?>',
            y: <?php echo $region['total_certification']; ?>,
            number:<?php echo $region['total_certification']; ?>
        },
        <?php } } ?>
        ]
    }]
});
</script>